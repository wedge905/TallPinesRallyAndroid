<resources>

    <!-- Base application theme. -->
    <style name="TallPinesTheme" parent="android:Theme.Holo"/>
    <!--style name="AppTheme" parent="android:Theme.Holo">
    </style-->

</resources>
